# MODEL_PATH is REQUIRED; everything else is optional.

# Model (required, no default) + LoRA (optional, no default — omit for none)
MODEL_PATH=Wan-AI/Wan2.2-I2V-A14B-Diffusers
LORA_PATH=lightx2v/Wan2.2-Distill-Loras

# Server
#PORT=8000

# Stable HTTPS hostname via Cloudflare Tunnel (see docs/deploy-pods.md).
# SECURITY: sglang's diffusion server has NO built-in API auth — run
# tunnel-only (no published port) and protect the hostname with Cloudflare
# Access (service token). Do not expose port 8000 to the internet.
#CF_TUNNEL_TOKEN=

# Extra `sglang serve` flags appended verbatim, e.g.:
#   --attention-backend fa3 --enable-torch-compile true
#   --text-encoder-cpu-offload --dit-layerwise-offload
#SGLANG_EXTRA_ARGS=

# HuggingFace
#HF_TOKEN=            # only for gated repos
# HF_HOME defaults to /workspace/hf in the image (persistent volume)
